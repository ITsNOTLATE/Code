package LinkedList_Questions;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

public class Reverse_linked_list {
    public static void main(String[] args) {
       /* Node<Integer> head = null;
        head.data=10;
        Node<Integer> current=head;
        Node<Integer> prev=null;
        Node<Integer> next;
        if(head.data==null){
            return ;
        }else{
            while(current.next!=null){
                next=current.next;
                current.next=prev;
                prev=current;
                current=next;
            }
            head=current;
        }

        //Merge two linkedList
        Node<Integer> head1=null;
        Node<Integer> head2 = null;

        Node<Integer> h1=head1;
        Node<Integer> h2=head2;
        if(h1==null){
            return;
        }
        if(h2==null){
            return;
        }
        Node<Integer> head3;

        if(h1.data>h2.data) {
            Node<Integer> head3 = new Node<>(h2.data);
        }else {
            Node<Integer> head3 = new Node<>(h1.data);
        }
        while (h1.next!=null&&h2.next!=null){
            if(h1.data>h2.data){
                Node<Integer> newNode=new Node<>(h2.data);
            }else{
                Node<Integer> newNode=new Node<>(h1.data);
            }

        }*/
    }
}
